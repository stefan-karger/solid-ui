---
title: Kbd
description: Used to display textual user input from keyboard.
---

::::tab-group[preview]
:::tab[Preview]

<ComponentPreview name="kbd-demo" />

:::

:::tab[Code]

```file="~/registry/examples/kbd-demo" frame=none showLineNumbers

```

:::
::::

## Installation

### CLI

```package-exec
solidui-cli@latest add kbd
```

### Manual

Install the following dependencies:

```package-install
@kobalte/core
```

Copy and paste the following code into your project.

```file="~/registry/ui/kbd.tsx" showLineNumbers

```

## Usage

```tsx showLineNumbers
import { Kbd } from "~/components/ui/kbd";
```

```tsx showLineNumbers
<Kbd>Ctrl</Kbd>
```

## Composition

Use the following composition to build `Kbd` and `KbdGroup`:

```text
Kbd
KbdGroup
├── Kbd
└── Kbd
```

## Examples

### Group

Use the `KbdGroup` component to group keyboard keys together.

::::tab-group[example1]
:::tab[Preview]

<ComponentPreview name="kbd-group" />

:::
:::tab[Code]

```file="~/registry/examples/kbd-group" frame=none showLineNumbers

```

:::
::::

### Button

Use the `Kbd` component inside a `Button` component to display a keyboard key inside a button.

::::tab-group[example2]
:::tab[Preview]

<ComponentPreview name="kbd-button" />

:::
:::tab[Code]

```file="~/registry/examples/kbd-button" frame=none showLineNumbers

```

:::
::::

### Tooltip

You can use the `Kbd` component inside a `Tooltip` component to display a tooltip with a keyboard key.

::::tab-group[example3]
:::tab[Preview]

<ComponentPreview name="kbd-tooltip" />

:::
:::tab[Code]

```file="~/registry/examples/kbd-tooltip" frame=none showLineNumbers

```

:::
::::

### Input Group

You can use the `Kbd` component inside a `InputGroupAddon` component to display a keyboard key inside an input group.

::::tab-group[example4]
:::tab[Preview]

<ComponentPreview name="kbd-input-group" />

:::
:::tab[Code]

```file="~/registry/examples/kbd-input-group" frame=none showLineNumbers

```

:::
::::

## API Reference

### Kbd

Use the `Kbd` component to display a keyboard key.

| Prop    | Type     | Default |
| ------- | -------- | ------- |
| `class` | `string` | ``      |

```tsx
<Kbd>Ctrl</Kbd>
```

### KbdGroup

Use the `KbdGroup` component to group `Kbd` components together.

| Prop    | Type     | Default |
| ------- | -------- | ------- |
| `class` | `string` | ``      |

```tsx
<KbdGroup>
  <Kbd>Ctrl</Kbd>
  <Kbd>B</Kbd>
</KbdGroup>
```
