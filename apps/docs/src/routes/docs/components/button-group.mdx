---
title: Button Group
description: A container that groups related buttons together with consistent styling.
---

::::tab-group[preview]
:::tab[Preview]

<ComponentPreview name="button-group-demo" />

:::
:::tab[Code]

```file="~/registry/v1/examples/button-group-demo" frame=none showLineNumbers

```

:::
::::

## Usage
```tsx
import {
  ButtonGroup,
  ButtonGroupSeparator,
  ButtonGroupText,
} from "@/components/ui/button-group"
```

```tsx
<ButtonGroup>
  <Button>Button 1</Button>
  <Button>Button 2</Button>
</ButtonGroup>
```

## Accessibility

- The `ButtonGroup` component has the `role` attribute set to `group`.
- Use <Kbd>Tab</Kbd> to navigate between the buttons in the group.
- Use `aria-label` or `aria-labelledby` to label the button group.

```tsx showLineNumbers
<ButtonGroup aria-label="Button group">
  <Button>Button 1</Button>
  <Button>Button 2</Button>
</ButtonGroup>
```

## ButtonGroup vs ToggleGroup

- Use the `ButtonGroup` component when you want to group buttons that perform an action.
- Use the `ToggleGroup` component when you want to group buttons that toggle a state.

## Examples

### Orientation

Set the `orientation` prop to change the button group layout.

::::tab-group[example-orientation]
:::tab[Preview]

<ComponentPreview name="button-group-orientation" />

:::
:::tab[Code]

```file="~/registry/v1/examples/button-group-orientation" frame=none showLineNumbers

```

:::
::::

### Size

Control the size of buttons using the `size` prop on individual buttons.

::::tab-group[example-size]
:::tab[Preview]

<ComponentPreview name="button-group-size" />

:::
:::tab[Code]

```file="~/registry/v1/examples/button-group-size" frame=none showLineNumbers

```

:::
::::

### Nested

Nest `<ButtonGroup>` components to create button groups with spacing.

::::tab-group[example-nested]
:::tab[Preview]

<ComponentPreview name="button-group-nested" />

:::
:::tab[Code]

```file="~/registry/v1/examples/button-group-nested" frame=none showLineNumbers

```

:::
::::

### Separator

The `ButtonGroupSeparator` component visually divides buttons within a group.

Buttons with variant `outline` do not need a separator since they have a border. For other variants, a separator is recommended to improve the visual hierarchy.

::::tab-group[example-separator]
:::tab[Preview]

<ComponentPreview name="button-group-separator" />

:::
:::tab[Code]

```file="~/registry/v1/examples/button-group-separator" frame=none showLineNumbers

```

:::
::::

### Split

Create a split button group by adding two buttons separated by a `ButtonGroupSeparator`.

::::tab-group[example-split]
:::tab[Preview]

<ComponentPreview name="button-group-split" />

:::
:::tab[Code]

```file="~/registry/v1/examples/button-group-split" frame=none showLineNumbers

```

:::
::::

### Input

Wrap an `Input` component with buttons.

::::tab-group[example-input]
:::tab[Preview]

<ComponentPreview name="button-group-input" />

:::
:::tab[Code]

```file="~/registry/v1/examples/button-group-input" frame=none showLineNumbers

```

:::
::::

### Input Group

Wrap an `InputGroup` component to create complex input layouts.

::::tab-group[example-input-group]
:::tab[Preview]

<ComponentPreview name="button-group-input-group" />

:::
:::tab[Code]

```file="~/registry/v1/examples/button-group-input-group" frame=none showLineNumbers

```

:::
::::

### Dropdown Menu

Create a split button group with a `DropdownMenu` component.

::::tab-group[example-dropdown-menu]
:::tab[Preview]

<ComponentPreview name="button-group-dropdown-menu" />

:::
:::tab[Code]

```file="~/registry/v1/examples/button-group-dropdown-menu" frame=none showLineNumbers

```

:::
::::

### Select

Pair with a `Select` component.

::::tab-group[example-select]
:::tab[Preview]

<ComponentPreview name="button-group-select" />

:::
:::tab[Code]

```file="~/registry/v1/examples/button-group-select" frame=none showLineNumbers

```

:::
::::

### Popover

Use with a `Popover` component.

::::tab-group[example-popover]
:::tab[Preview]

<ComponentPreview name="button-group-popover" />

:::
:::tab[Code]

```file="~/registry/v1/examples/button-group-popover" frame=none showLineNumbers

```

:::
::::

## API Reference

### ButtonGroup

The `ButtonGroup` component is a container that groups related buttons together with consistent styling.

| Prop          | Type                         | Default        |
| ------------- | ---------------------------- | -------------- |
| `orientation` | `"horizontal" \| "vertical"` | `"horizontal"` |

```tsx
<ButtonGroup>
  <Button>Button 1</Button>
  <Button>Button 2</Button>
</ButtonGroup>
```

Nest multiple button groups to create complex layouts with spacing. See the [nested](#nested) example for more details.

```tsx
<ButtonGroup>
  <ButtonGroup />
  <ButtonGroup />
</ButtonGroup>
```

### ButtonGroupSeparator

The `ButtonGroupSeparator` component visually divides buttons within a group.

| Prop          | Type                         | Default      |
| ------------- | ---------------------------- | ------------ |
| `orientation` | `"horizontal" \| "vertical"` | `"vertical"` |

```tsx
<ButtonGroup>
  <Button>Button 1</Button>
  <ButtonGroupSeparator />
  <Button>Button 2</Button>
</ButtonGroup>
```

### ButtonGroupText

Use this component to display text within a button group.

```tsx
<ButtonGroup>
  <ButtonGroupText>Text</ButtonGroupText>
  <Button>Button</Button>
</ButtonGroup>
```

Use the `as` prop to render a custom component as the text, for example a label.

```tsx showLineNumbers
import { ButtonGroupText } from "~/components/ui/button-group"
import { Label } from "~/components/ui/label"

export function ButtonGroupTextDemo() {
  return (
    <ButtonGroup>
      <Label as={ButtonGroupText} htmlFor="name">Text</Label>
      <Input placeholder="Type something here..." id="name" />
    </ButtonGroup>
  )
}
```
