---
title: Card
description: Displays a card with header, content, and footer.
---

::::tab-group[preview]
:::tab[Preview]

<ComponentPreview name="card-demo" />

:::
:::tab[Code]

```file="~/registry/examples/card-demo" frame=none showLineNumbers

```

:::
::::

## Installation

### CLI

```package-exec
solidui-cli@latest add card
```

### Manual

Copy and paste the following code into your project.

```file="~/registry/ui/card.tsx" showLineNumbers

```

## Usage

```tsx showLineNumbers
import {
  Card,
  CardAction,
  CardContent,
  CardDescription,
  CardFooter,
  CardHeader,
  CardTitle,
} from "~/components/ui/card";
```

```tsx showLineNumbers
<Card>
  <CardHeader>
    <CardTitle>Card Title</CardTitle>
    <CardDescription>Card Description</CardDescription>
    <CardAction>Card Action</CardAction>
  </CardHeader>
  <CardContent>
    <p>Card Content</p>
  </CardContent>
  <CardFooter>
    <p>Card Footer</p>
  </CardFooter>
</Card>
```

## Composition

Use the following composition to build a `Card`:

```text
Card
├── CardHeader
│   ├── CardTitle
│   ├── CardDescription
│   └── CardAction
├── CardContent
└── CardFooter
```

## Examples

### Size

Use the `size="sm"` prop to set the card to its compact size.

::::tab-group[example1]
:::tab[Preview]

<ComponentPreview name="card-small" previewClass="min-h-96" />

:::
:::tab[Code]

```file="~/registry/examples/card-small" frame=none showLineNumbers

```

:::
::::

### Spacing

Use the `--card-spacing` CSS variable to control the gap between sections and the inset of card parts.

::::tab-group[example2]
:::tab[Preview]

<ComponentPreview name="card-spacing" previewClass="min-h-[30rem]" />

:::
:::tab[Code]

```file="~/registry/examples/card-spacing" frame=none showLineNumbers

```

:::
::::

Use negative margins with `-mx-(--card-spacing)` to make content edge-to-edge while keeping it aligned with the card inset.

::::tab-group[example3]
:::tab[Preview]

<ComponentPreview name="card-edge-to-edge" previewClass="min-h-[24rem]" />

:::
:::tab[Code]

```file="~/registry/examples/card-edge-to-edge" frame=none showLineNumbers

```

:::
::::

### Image

Add an image before the card header to create a card with an edge-to-edge image.

::::tab-group[example4]
:::tab[Preview]

<ComponentPreview name="card-image" previewClass="min-h-[30rem]" />

:::
:::tab[Code]

```file="~/registry/examples/card-image" frame=none showLineNumbers

```

:::
::::

## API Reference

### Card

The root container for card content.

| Prop    | Type                | Default     |
| ------- | ------------------- | ----------- |
| `size`  | `"default" \| "sm"` | `"default"` |
| `class` | `string`            | -           |

### CardHeader

Contains a title, description, and optional action.

| Prop    | Type     | Default |
| ------- | -------- | ------- |
| `class` | `string` | -       |

### CardTitle

The card title.

| Prop    | Type     | Default |
| ------- | -------- | ------- |
| `class` | `string` | -       |

### CardDescription

Helper text displayed under the title.

| Prop    | Type     | Default |
| ------- | -------- | ------- |
| `class` | `string` | -       |

### CardAction

Places content in the top-right of the header.

| Prop    | Type     | Default |
| ------- | -------- | ------- |
| `class` | `string` | -       |

### CardContent

The main card body.

| Prop    | Type     | Default |
| ------- | -------- | ------- |
| `class` | `string` | -       |

### CardFooter

Actions or secondary content displayed at the bottom of the card.

| Prop    | Type     | Default |
| ------- | -------- | ------- |
| `class` | `string` | -       |
