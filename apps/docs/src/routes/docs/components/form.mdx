---
title: Form
description: Building forms with Modular Forms integration for validation and state management.
---

::::tab-group[preview]
:::tab[Preview]

<ComponentPreview name="form-demo" />

:::

:::tab[Code]

```file="~/registry/examples/form-demo" frame=none showLineNumbers

```

:::
::::

## Installation

### CLI

```package-exec
solidui-cli@latest add form
```

### Manual

Install the following dependencies:

```package-install
@modular-forms/solid
```

Copy and paste the following code into your project.

```file="~/registry/ui/form.tsx" showLineNumbers

```

This component uses the `label` component from the registry. Make sure you have it installed.

## Usage

```tsx
import {
  Form,
  FormControl,
  FormDescription,
  FormErrorMessage,
  FormField,
  FormLabel,
} from "~/components/ui/form";
```

```tsx
<Form>
  <FormField name="username">
    <FormLabel>Username</FormLabel>
    <FormControl>
      <input type="text" />
    </FormControl>
    <FormDescription>Enter your username</FormDescription>
    <FormErrorMessage>Error message</FormErrorMessage>
  </FormField>
</Form>
```
