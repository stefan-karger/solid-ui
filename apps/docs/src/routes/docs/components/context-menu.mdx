---
title: Context Menu
description: Displays a menu to the user — such as a set of actions or functions — triggered by a right-click or long-press.
links:
  doc: https://kobalte.dev/docs/core/components/context-menu
  api: https://kobalte.dev/docs/core/components/context-menu#api-reference
---

The Context Menu component is built on top of [Kobalte Context Menu](https://kobalte.dev/docs/core/components/context-menu), which provides accessible context-menu, keyboard, and focus behavior.

::::tab-group[preview]
:::tab[Preview]

<ComponentPreview name="context-menu-demo" />

:::
:::tab[Code]

```file="~/registry/examples/context-menu-demo" frame=none showLineNumbers

```

:::
::::

## Installation

### CLI

```package-exec
solidui-cli@latest add context-menu
```

### Manual

Install the following dependencies:

```package-install
@kobalte/core
```

Copy and paste the following code into your project.

```file="~/registry/ui/context-menu.tsx" showLineNumbers

```

## Usage

```tsx showLineNumbers
import {
  ContextMenu,
  ContextMenuContent,
  ContextMenuItem,
  ContextMenuTrigger,
} from "~/components/ui/context-menu";
```

```tsx showLineNumbers
<ContextMenu>
  <ContextMenuTrigger>Right click here</ContextMenuTrigger>
  <ContextMenuContent>
    <ContextMenuItem>Profile</ContextMenuItem>
    <ContextMenuItem>Billing</ContextMenuItem>
    <ContextMenuItem>Team</ContextMenuItem>
    <ContextMenuItem>Subscription</ContextMenuItem>
  </ContextMenuContent>
</ContextMenu>
```

See the [Kobalte Context Menu documentation](https://kobalte.dev/docs/core/components/context-menu) for primitive-level options, accessibility, and keyboard behavior.

## Composition

Use the following composition to build a `ContextMenu`:

```text
ContextMenu
├── ContextMenuTrigger
└── ContextMenuContent
    ├── ContextMenuGroup
    │   ├── ContextMenuLabel
    │   ├── ContextMenuItem
    │   └── ContextMenuItem
    ├── ContextMenuSeparator
    ├── ContextMenuGroup
    │   ├── ContextMenuLabel
    │   ├── ContextMenuCheckboxItem
    │   └── ContextMenuCheckboxItem
    ├── ContextMenuSeparator
    ├── ContextMenuGroup
    │   ├── ContextMenuLabel
    │   └── ContextMenuRadioGroup
    │       ├── ContextMenuRadioItem
    │       └── ContextMenuRadioItem
    └── ContextMenuSub
        ├── ContextMenuSubTrigger
        └── ContextMenuSubContent
            └── ContextMenuGroup
                ├── ContextMenuItem
                └── ContextMenuItem
```

## Examples

### Basic

A simple context menu with a few actions.

::::tab-group[example1]
:::tab[Preview]

<ComponentPreview name="context-menu-basic" />

:::
:::tab[Code]

```file="~/registry/examples/context-menu-basic" frame=none showLineNumbers

```

:::
::::

### Submenu

Use `ContextMenuSub` to nest secondary actions.

::::tab-group[example2]
:::tab[Preview]

<ComponentPreview name="context-menu-submenu" />

:::
:::tab[Code]

```file="~/registry/examples/context-menu-submenu" frame=none showLineNumbers

```

:::
::::

### Shortcuts

Add `ContextMenuShortcut` to show keyboard hints.

::::tab-group[example3]
:::tab[Preview]

<ComponentPreview name="context-menu-shortcuts" />

:::
:::tab[Code]

```file="~/registry/examples/context-menu-shortcuts" frame=none showLineNumbers

```

:::
::::

### Groups

Group related actions and separate them with dividers.

::::tab-group[example4]
:::tab[Preview]

<ComponentPreview name="context-menu-groups" />

:::
:::tab[Code]

```file="~/registry/examples/context-menu-groups" frame=none showLineNumbers

```

:::
::::

### Icons

Combine icons with labels for quick scanning.

::::tab-group[example5]
:::tab[Preview]

<ComponentPreview name="context-menu-icons" />

:::
:::tab[Code]

```file="~/registry/examples/context-menu-icons" frame=none showLineNumbers

```

:::
::::

### Checkboxes

Use `ContextMenuCheckboxItem` for toggles.

::::tab-group[example6]
:::tab[Preview]

<ComponentPreview name="context-menu-checkboxes" />

:::
:::tab[Code]

```file="~/registry/examples/context-menu-checkboxes" frame=none showLineNumbers

```

:::
::::

### Radio

Use `ContextMenuRadioItem` for exclusive choices.

::::tab-group[example7]
:::tab[Preview]

<ComponentPreview name="context-menu-radio" />

:::
:::tab[Code]

```file="~/registry/examples/context-menu-radio" frame=none showLineNumbers

```

:::
::::

### Destructive

Use `variant="destructive"` to style the menu item as destructive.

::::tab-group[example8]
:::tab[Preview]

<ComponentPreview name="context-menu-destructive" />

:::
:::tab[Code]

```file="~/registry/examples/context-menu-destructive" frame=none showLineNumbers

```

:::
::::

### Sides

Control menu placement by passing a `placement` prop to `ContextMenu`.

::::tab-group[example9]
:::tab[Preview]

<ComponentPreview name="context-menu-sides" />

:::
:::tab[Code]

```file="~/registry/examples/context-menu-sides" frame=none showLineNumbers

```

:::
::::

## API Reference

See the [Kobalte Context Menu API reference](https://kobalte.dev/docs/core/components/context-menu#api-reference) for primitive props, keyboard behavior, and accessibility details.
