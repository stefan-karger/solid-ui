---
title: Tooltip
description: A popup that displays information related to an element when the element receives keyboard focus or the mouse hovers over it.
---

The Tooltip component is built on top of [Kobalte Tooltip](https://kobalte.dev/docs/core/components/tooltip), which provides accessible hover, focus, and keyboard behavior.

::::tab-group[preview]
:::tab[Preview]

<ComponentPreview name="tooltip-demo" />

:::

:::tab[Code]

```file="~/registry/examples/tooltip-demo" frame=none showLineNumbers

```

:::
::::

## Installation

### CLI

```package-exec
solidui-cli@latest add tooltip
```

### Manual

Install the following dependencies:

```package-install
@kobalte/core
```

Copy and paste the following code into your project.

```file="~/registry/ui/tooltip.tsx" showLineNumbers

```

## Usage

```tsx showLineNumbers
import { Button } from "~/components/ui/button";
import {
  Tooltip,
  TooltipContent,
  TooltipTrigger,
} from "~/components/ui/tooltip";
```

```tsx showLineNumbers
<Tooltip>
  <TooltipTrigger as={Button} variant="outline">
    Hover
  </TooltipTrigger>
  <TooltipContent>
    <p>Add to library</p>
  </TooltipContent>
</Tooltip>
```

## Composition

Use the following composition to build a `Tooltip`:

```text
Tooltip
├── TooltipTrigger
└── TooltipContent
```

## Examples

### Placement

Use the `placement` prop to change the position of the tooltip.

<ComponentPreview name="tooltip-placement" />

### With Keyboard Shortcut

<ComponentPreview name="tooltip-keyboard" />

### Disabled Button

Show a tooltip on a disabled button by wrapping it with a span.

<ComponentPreview name="tooltip-disabled" />

## API Reference

See the [Kobalte Tooltip API reference](https://kobalte.dev/docs/core/components/tooltip#api-reference) for primitive props, keyboard behavior, and accessibility details.
